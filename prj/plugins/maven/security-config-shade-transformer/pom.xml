<?xml version="1.0"?>
<!--
  Copyright (c) 2000, 2026, Oracle and/or its affiliates.

  Licensed under the Universal Permissive License v 1.0 as shown at
  https://oss.oracle.com/licenses/upl.
  -->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.oracle.coherence.ce</groupId>
    <artifactId>maven</artifactId>
    <version>${revision}</version>
    <relativePath>../pom.xml</relativePath>
  </parent>

  <artifactId>security-config-shade-transformer</artifactId>

  <name>Coherence Security Config Shade Transformer</name>
  <description>Maven Shade transformer that merges Coherence security-config.xml resources</description>

  <properties>
    <!-- this module should be published -->
    <maven.deploy.skip>false</maven.deploy.skip>
    <!-- use a distinct provider realm when this standalone module builds beside the CE root -->
    <maven.gmaven.providerSelection.version>1.6</maven.gmaven.providerSelection.version>
    <!-- this module uses JUnit only, so avoid Mockito javaagent startup failures -->
    <default.surefire.argLine>-Xshare:off -Djava.net.preferIPv4Stack=true -Xmx2g -XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath=${project.build.directory} -XX:+ExitOnOutOfMemoryError -Dfile.encoding=UTF-8</default.surefire.argLine>
    <surefire.argLine>${default.surefire.argLine}</surefire.argLine>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.apache.maven.plugins</groupId>
      <artifactId>maven-shade-plugin</artifactId>
      <version>${maven.shade.plugin.version}</version>
      <scope>provided</scope>
    </dependency>

    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
