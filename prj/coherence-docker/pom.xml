<?xml version="1.0" encoding="UTF-8"?>
<!--
  Copyright (c) 2000, 2020, Oracle and/or its affiliates.

  Licensed under the Universal Permissive License v 1.0 as shown at
  http://oss.oracle.com/licenses/upl.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.oracle.coherence.ce</groupId>
    <artifactId>main</artifactId>
    <version>14.1.1-0-2-SNAPSHOT</version>
    <relativePath>../pom.xml</relativePath>
  </parent>

  <artifactId>coherence-docker</artifactId>
  <packaging>jar</packaging>
  <name>Coherence Docker Distribution</name>

  <properties>
    <docker.registry>oraclecoherence</docker.registry>
    <docker.image.name>${docker.registry}/coherence-ce:${project.version}</docker.image.name>

    <!-- The base image for the Coherence image - usually a distroless OpenJDK image -->
    <docker.base.image.name>gcr.io/distroless/java:11</docker.base.image.name>

    <port.extend>20000</port.extend>
    <port.management>30000</port.management>
    <port.metrics>9612</port.metrics>
  </properties>

  <dependencies>
    <dependency>
      <groupId>${coherence.group.id}</groupId>
      <artifactId>coherence</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>${coherence.group.id}</groupId>
      <artifactId>coherence-management</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>${coherence.group.id}</groupId>
      <artifactId>coherence-metrics</artifactId>
      <version>${project.version}</version>
    </dependency>

    <dependency>
      <groupId>io.jaegertracing</groupId>
      <artifactId>jaeger-client</artifactId>
    </dependency>

    <!-- test dependencies -->
    <dependency>
      <groupId>${coherence.group.id}</groupId>
      <artifactId>coherence-testing-support</artifactId>
      <version>${project.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.oracle.bedrock</groupId>
      <artifactId>bedrock-runtime</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.oracle.bedrock</groupId>
      <artifactId>bedrock-testing-support</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.oracle.bedrock.coherence</groupId>
      <artifactId>coherence-12.2.1-testing-support</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.oracle.bedrock</groupId>
      <artifactId>bedrock-runtime-docker</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.hamcrest</groupId>
      <artifactId>hamcrest-all</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.mockito</groupId>
      <artifactId>mockito-core</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-failsafe-plugin</artifactId>
        <configuration>
          <skip>true</skip>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <skip>true</skip>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile>
      <id>docker</id>
      <activation>
        <activeByDefault>false</activeByDefault>
      </activation>
      <build>
        <plugins>
          <plugin>
             <groupId>org.codehaus.mojo</groupId>
             <artifactId>exec-maven-plugin</artifactId>
             <executions>
               <execution>
                 <id>pull-base-image</id>
                 <goals>
                   <goal>exec</goal>
                 </goals>
                 <phase>prepare-package</phase>
                 <configuration>
                   <executable>docker</executable>
                   <arguments>
                     <argument>pull</argument>
                     <argument>${docker.base.image.name}</argument>
                   </arguments>
                 </configuration>
               </execution>
             </executions>
           </plugin>

          <plugin>
            <groupId>com.google.cloud.tools</groupId>
            <artifactId>jib-maven-plugin</artifactId>
            <executions>
              <execution>
                <goals>
                  <goal>dockerBuild</goal>
                </goals>
                <phase>package</phase>
                <configuration>
                  <from>
                    <image>docker://${docker.base.image.name}</image>
                  </from>
                  <to>
                    <image>${docker.image.name}</image>
                  </to>
                  <extraDirectories>
                    <paths>
                      <path>
                        <from>${project.basedir}/src/main/args</from>
                        <into>/args</into>
                      </path>
                    </paths>
                  </extraDirectories>
                  <container>
                    <mainClass>com.tangosol.coherence.docker.Runner</mainClass>
                    <environment>
                      <!-- Jaeger tracing variables -->
                      <JAEGER_SAMPLER_TYPE>const</JAEGER_SAMPLER_TYPE>
                      <JAEGER_SAMPLER_PARAM>1</JAEGER_SAMPLER_PARAM>
                      <JAEGER_SERVICE_NAME>coherence</JAEGER_SERVICE_NAME>
                    </environment>
                    <extraClasspath>
                      <classpath>/coherence/ext/conf</classpath>
                      <classpath>/coherence/ext/lib/*</classpath>
                    </extraClasspath>
                    <jvmFlags>
                      <jvmFlag>-XshowSettings:all</jvmFlag>
                      <jvmFlag>-XX:+PrintCommandLineFlags</jvmFlag>
                      <jvmFlag>-XX:+PrintFlagsFinal</jvmFlag>
                      <jvmFlag>-Dcoherence.log=jdk</jvmFlag>
                      <jvmFlag>-Dcoherence.management.http=all</jvmFlag>
                      <jvmFlag>-Dcoherence.management.http.port=${port.management}</jvmFlag>
                      <jvmFlag>-Dcoherence.extend.port=${port.extend}</jvmFlag>
                      <jvmFlag>-Dcoherence.metrics.http.enabled=true</jvmFlag>
                      <jvmFlag>-Dcoherence.metrics.http.port=${port.metrics}</jvmFlag>
                      <jvmFlag>-Dcoherence.ttl=0</jvmFlag>
                      <jvmFlag>-Dcoherence.tracing.ratio=1</jvmFlag>
                      <jvmFlag>@/args/jvm-args.txt</jvmFlag>
                    </jvmFlags>
                    <ports>
                      <port>${port.metrics}</port>
                      <port>${port.management}</port>
                      <port>${port.extend}</port>
                    </ports>
                  </container>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>

    <profile>
      <id>docker-test</id>
      <activation>
        <activeByDefault>false</activeByDefault>
      </activation>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-failsafe-plugin</artifactId>
            <configuration>
              <skip>false</skip>
              <skipTests>false</skipTests>
              <systemProperties>
                <docker.image.name>${docker.image.name}</docker.image.name>
                <port.extend>${port.extend}</port.extend>
                <port.management>${port.management}</port.management>
                <port.metrics>${port.metrics}</port.metrics>
              </systemProperties>
            </configuration>
          </plugin>

          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-surefire-plugin</artifactId>
            <configuration>
              <systemProperties>
                <skip>false</skip>
                <skipTests>false</skipTests>
                <docker.image.name>${docker.image.name}</docker.image.name>
                <port.extend>${port.extend}</port.extend>
                <port.management>${port.management}</port.management>
                <port.metrics>${port.metrics}</port.metrics>
              </systemProperties>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>

    <profile>
      <id>docker-push</id>
      <activation>
        <activeByDefault>false</activeByDefault>
      </activation>
      <build>
        <plugins>
          <plugin>
             <groupId>org.codehaus.mojo</groupId>
             <artifactId>exec-maven-plugin</artifactId>
             <executions>
               <execution>
                 <id>push-image</id>
                 <goals>
                   <goal>exec</goal>
                 </goals>
                 <configuration>
                   <executable>docker</executable>
                   <arguments>
                     <argument>push</argument>
                     <argument>${docker.image.name}</argument>
                   </arguments>
                 </configuration>
               </execution>
             </executions>
           </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
