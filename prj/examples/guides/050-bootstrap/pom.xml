<?xml version="1.0" encoding="UTF-8"?>
<!--
  Copyright (c) 2000, 2026, Oracle and/or its affiliates.

  Licensed under the Universal Permissive License v 1.0 as shown at
  https://oss.oracle.com/licenses/upl.
  -->

<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.oracle.coherence.guides</groupId>
  <artifactId>bootstrap</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <name>Coherence Guides - Bootstrap API</name>

  <properties>
    <revision>26.10-SNAPSHOT</revision>

    <coherence.group.id>com.oracle.coherence.ce</coherence.group.id>
    <coherence.version>${revision}</coherence.version>

    <junit.jupiter.version>5.12.2</junit.jupiter.version>

    <java.version>21</java.version>

    <maven.compiler.source>${java.version}</maven.compiler.source>
    <maven.compiler.target>${java.version}</maven.compiler.target>

    <maven.failsafe.plugin.version>3.5.3</maven.failsafe.plugin.version>
    <maven.surefire.plugin.version>3.5.3</maven.surefire.plugin.version>

    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    <maven.build.timestamp.format>yyyyMMddHHmmssSSS</maven.build.timestamp.format>
    <coherence.cluster>example-${project.artifactId}-${maven.build.timestamp}</coherence.cluster>
    <coherence.ttl>0</coherence.ttl>
    <coherence.wka>127.0.0.1</coherence.wka>
    <coherence.localhost>127.0.0.1</coherence.localhost>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>${coherence.group.id}</groupId>
        <artifactId>coherence-bom</artifactId>
        <version>${coherence.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>${coherence.group.id}</groupId>
      <artifactId>coherence</artifactId>
    </dependency>

    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter-api</artifactId>
      <version>${junit.jupiter.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-failsafe-plugin</artifactId>
        <version>${maven.failsafe.plugin.version}</version>
        <configuration>
          <systemPropertyVariables>
            <coherence.cluster>${coherence.cluster}</coherence.cluster>
            <coherence.ttl>${coherence.ttl}</coherence.ttl>
            <coherence.wka>${coherence.wka}</coherence.wka>
            <coherence.localhost>${coherence.localhost}</coherence.localhost>
          </systemPropertyVariables>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>${maven.surefire.plugin.version}</version>
        <configuration>
          <systemPropertyVariables>
            <coherence.cluster>${coherence.cluster}</coherence.cluster>
            <coherence.ttl>${coherence.ttl}</coherence.ttl>
            <coherence.wka>${coherence.wka}</coherence.wka>
            <coherence.localhost>${coherence.localhost}</coherence.localhost>
          </systemPropertyVariables>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
